// Minimal open-next.config.ts for Cloudflare deployment
import { defineCloudflareConfig } from "@opennextjs/cloudflare/config";

export default defineCloudflareConfig({
	// Use minimal configuration to handle build issues
});
