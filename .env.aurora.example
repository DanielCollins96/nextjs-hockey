# Aurora Serverless Data API Configuration
# Copy this to .env.local and fill in your values

# Set to "true" to use Aurora instead of Supabase
USE_AURORA=true

# Your Aurora Serverless cluster ARN
# Format: arn:aws:rds:<region>:<account-id>:cluster:<cluster-name>
AURORA_CLUSTER_ARN=arn:aws:rds:us-east-1:123456789012:cluster:your-cluster-name

# Your Secrets Manager secret ARN (contains DB credentials)
# Format: arn:aws:secretsmanager:<region>:<account-id>:secret:<secret-name>
AURORA_SECRET_ARN=arn:aws:secretsmanager:us-east-1:123456789012:secret:your-secret-name

# Database name (default: postgres)
AURORA_DATABASE=postgres

# AWS Region (default: us-east-1)
AURORA_REGION=us-east-1

# Note: AWS credentials are picked up automatically from:
# - Environment variables (AWS_ACCESS_KEY_ID, AWS_SECRET_ACCESS_KEY)
# - AWS credentials file (~/.aws/credentials)
# - IAM role (if running on AWS infrastructure)
